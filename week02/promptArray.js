let names =[]
let i=0
while (true) {
    let input = prompt("Type in your name.\nType'exit'to display all names");
    if (input === 'exit') {
        break;
        {else}
        names.push (input);
    }
};
